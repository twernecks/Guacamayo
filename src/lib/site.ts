/**
 * Placeholder production URL pending the pousada's real domain. Update via
 * NEXT_PUBLIC_SITE_URL (or replace the fallback) before publishing so
 * metadata, the sitemap and canonical URLs point to the live site.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://example.com";
