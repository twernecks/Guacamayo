/**
 * Defaults to the GitHub Pages project page for this repo. Override via
 * NEXT_PUBLIC_SITE_URL once the pousada has a real custom domain, so
 * metadata, the sitemap and canonical URLs point to the live site.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://twernecks.github.io/Guacamayo";
